# 🔧 配置指南和故障排除

## ⚠️ 编译错误修复

### 错误 1: 未能找到程序集 "Wolfram.NETLink"

**错误信息：**
```
Warning MSB3245 : 未能解析此引用。未能找到程序集"Wolfram.NETLink"。
```

**原因：** 项目文件中的 Wolfram.NETLink.dll 路径不正确。

**解决方案：**

#### 步骤 1: 找到 Wolfram.NETLink.dll

在你的系统中找到 Mathematica 安装目录，通常在：

**Windows:**
```
C:\Program Files\Wolfram Research\Mathematica\14.3\SystemFiles\Links\NETLink\Wolfram.NETLink.dll
```

**macOS:**
```
/Applications/Mathematica.app/Contents/SystemFiles/Links/NETLink/Wolfram.NETLink.dll
```

**Linux:**
```
/usr/local/Wolfram/Mathematica/14.3/SystemFiles/Links/NETLink/Wolfram.NETLink.dll
```

#### 步骤 2: 修改项目文件

打开 `MathematicaDemo.csproj`，找到这一行：

```xml
<Reference Include="Wolfram.NETLink">
  <HintPath>C:\Program Files\Wolfram Research\Mathematica\14.3\SystemFiles\Links\NETLink\Wolfram.NETLink.dll</HintPath>
</Reference>
```

将 `<HintPath>` 中的路径修改为你的实际路径。

**示例修改：**

如果你的 Mathematica 安装在 `D:\Mathematica\14.3`，则修改为：
```xml
<Reference Include="Wolfram.NETLink">
  <HintPath>D:\Mathematica\14.3\SystemFiles\Links\NETLink\Wolfram.NETLink.dll</HintPath>
</Reference>
```

#### 步骤 3: 验证文件存在

在命令行中检查文件是否存在：

**Windows:**
```cmd
dir "C:\Program Files\Wolfram Research\Mathematica\14.3\SystemFiles\Links\NETLink\Wolfram.NETLink.dll"
```

**Linux/Mac:**
```bash
ls -l /Applications/Mathematica.app/Contents/SystemFiles/Links/NETLink/Wolfram.NETLink.dll
```

如果文件不存在，说明路径不对或者 Mathematica 未正确安装。

---

### 错误 2: 内插字符串大括号转义错误

**错误信息：**
```
Error CS8086 : 在内插字符串中，必需对"}"字符进行转义(通过加倍)。
```

**原因：** 在 C# 内插字符串 `$"..."` 中，大括号 `{` 和 `}` 需要转义。

**已修复：** 我已经在最新版本中修复了这个问题。

**修复前：**
```csharp
Console.WriteLine($"矩阵 {{1,2},{3,4}} 的行列式: {detResult}");
```

**修复后：**
```csharp
Console.WriteLine("矩阵 {{1,2},{3,4}} 的行列式: " + detResult);
```

---

## 📝 完整配置步骤

### 1️⃣ 安装必备软件

✅ **Mathematica 14.3 或更高版本**
- 确保已激活许可证
- 记住安装路径

✅ **.NET 8.0 SDK**
- 下载：https://dotnet.microsoft.com/download/dotnet/8.0
- 验证安装：`dotnet --version`

### 2️⃣ 修改配置文件

#### 修改 MathematicaDemo.csproj

找到并修改以下部分：

```xml
<ItemGroup>
  <!-- 修改这里的路径 -->
  <Reference Include="Wolfram.NETLink">
    <HintPath>你的Mathematica路径\SystemFiles\Links\NETLink\Wolfram.NETLink.dll</HintPath>
  </Reference>
</ItemGroup>
```

#### 修改 MathematicaHelper.cs

找到并修改 `Initialize` 方法中的路径：

```csharp
if (string.IsNullOrEmpty(mathematicaPath))
{
    // 修改这里的路径
    mathematicaPath = @"你的Mathematica路径\MathKernel.exe";
}
```

**Windows 示例：**
```csharp
mathematicaPath = @"C:\Program Files\Wolfram Research\Mathematica\14.3\MathKernel.exe";
```

**macOS 示例：**
```csharp
mathematicaPath = "/Applications/Mathematica.app/Contents/MacOS/MathKernel";
```

**Linux 示例：**
```csharp
mathematicaPath = "/usr/local/Wolfram/Mathematica/14.3/Executables/MathKernel";
```

### 3️⃣ 编译和运行

```bash
# 清理之前的编译
dotnet clean

# 重新编译
dotnet build

# 如果编译成功，运行
dotnet run
```

---

## 🔍 常见路径查找方法

### Windows 系统

#### 方法 1: 使用资源管理器搜索
1. 打开 `此电脑`
2. 在搜索框输入 `Wolfram.NETLink.dll`
3. 等待搜索完成
4. 复制找到的文件路径

#### 方法 2: 使用命令行查找
```cmd
dir /s /b "C:\Wolfram.NETLink.dll"
```

#### 方法 3: 使用 PowerShell
```powershell
Get-ChildItem -Path "C:\Program Files" -Filter "Wolfram.NETLink.dll" -Recurse -ErrorAction SilentlyContinue
```

### macOS/Linux 系统

```bash
# 查找 DLL 文件
find /Applications -name "Wolfram.NETLink.dll" 2>/dev/null
find /usr/local -name "Wolfram.NETLink.dll" 2>/dev/null

# 查找 MathKernel
find /Applications -name "MathKernel" 2>/dev/null
find /usr/local -name "MathKernel" 2>/dev/null
```

---

## 🛠️ 故障排除清单

### 编译前检查

- [ ] Mathematica 已安装并激活
- [ ] .NET 8.0 SDK 已安装
- [ ] Wolfram.NETLink.dll 文件存在
- [ ] MathKernel.exe 文件存在
- [ ] 项目文件中的路径已修改
- [ ] MathematicaHelper.cs 中的路径已修改

### 编译时问题

**问题：** 找不到 Wolfram.NETLink
```
解决方案：
1. 检查 .csproj 文件中的 HintPath 是否正确
2. 确保路径使用反斜杠（Windows）或正斜杠（Linux/Mac）
3. 路径如果包含空格，确保使用引号
```

**问题：** 平台目标不匹配
```
解决方案：
在 .csproj 中添加：
<PropertyGroup>
  <PlatformTarget>x64</PlatformTarget>
</PropertyGroup>
```

**问题：** 语法错误
```
解决方案：
下载最新修复版本，已修复内插字符串转义问题
```

### 运行时问题

**问题：** 内核启动失败
```
解决方案：
1. 检查 MathematicaHelper.cs 中的 MathKernel 路径
2. 确保 Mathematica 许可证有效
3. 手动测试 MathKernel 是否能启动
```

**问题：** 函数包加载失败
```
解决方案：
1. 确保 MathematicaFunctions.m 在输出目录
2. 检查项目文件中的 CopyToOutputDirectory 设置
3. 手动复制 .m 文件到 bin/Debug/net8.0/
```

---

## 📦 推荐的项目结构

```
MathematicaDemo/
├── MathematicaDemo.csproj      ← 修改 DLL 引用路径
├── MathematicaHelper.cs        ← 修改 MathKernel 路径
├── Program.cs                  ← 已修复转义问题
├── MathematicaFunctions.m
└── bin/
    └── Debug/
        └── net8.0/
            ├── MathematicaDemo.dll
            ├── MathematicaDemo.exe
            └── MathematicaFunctions.m  ← 自动复制到这里
```

---

## 🎯 快速修复指南

### 5分钟快速配置

```bash
# 1. 找到 Mathematica 安装目录
# Windows: C:\Program Files\Wolfram Research\Mathematica\14.3
# macOS: /Applications/Mathematica.app
# Linux: /usr/local/Wolfram/Mathematica/14.3

# 2. 编辑 MathematicaDemo.csproj
# 修改 <HintPath> 为实际路径

# 3. 编辑 MathematicaHelper.cs
# 修改 mathematicaPath 为实际路径

# 4. 编译
dotnet clean
dotnet build

# 5. 运行
dotnet run
```

---

## 💻 不同版本的 Mathematica

如果你使用的不是 Mathematica 14.3：

### Mathematica 14.0
```
路径中的 14.3 改为 14.0
```

### Mathematica 13.x
```
路径中的 14.3 改为 13.x（如 13.3）
注意：API 可能有差异
```

### Mathematica 12.x
```
路径中的 14.3 改为 12.x
部分功能可能不兼容
```

---

## 🔑 许可证问题

如果遇到许可证相关错误：

```mathematica
(* 在 Mathematica 中检查许可证 *)
$LicenseExpirationDate
$ActivationKey
```

**常见许可证错误：**
- 许可证过期
- 网络激活失败
- 并发许可证被占用

**解决方案：**
1. 重新激活 Mathematica
2. 联系 Wolfram 技术支持
3. 检查网络连接

---

## 📞 获取帮助

### 检查系统信息

```bash
# .NET 版本
dotnet --version

# .NET SDK 列表
dotnet --list-sdks

# .NET Runtime 列表
dotnet --list-runtimes
```

### 生成诊断报告

```bash
# 详细编译信息
dotnet build --verbosity detailed > build.log

# 查看日志
cat build.log
```

### 测试 Mathematica

```bash
# Windows
"C:\Program Files\Wolfram Research\Mathematica\14.3\MathKernel.exe"

# macOS/Linux
/Applications/Mathematica.app/Contents/MacOS/MathKernel
```

在 Mathematica 中运行：
```mathematica
$Version
$MachineName
$ProcessorType
```

---

## ✅ 验证配置成功

如果看到以下输出，说明配置成功：

```
正在启动 Mathematica 内核...
Mathematica 内核启动成功！
自定义函数包加载成功！

【示例 1】单个整数参数，返回数值（同步）
--------------------------------------------------------------------------------
输入: 5
SquareNumber(5) = 25
```

---

## 📚 相关文档

- [Wolfram .NET/Link 文档](https://reference.wolfram.com/language/NETLink/)
- [.NET 8.0 文档](https://docs.microsoft.com/dotnet/core/whats-new/dotnet-8)
- [项目 README](README.md)

---

**最后更新**: 2025-11-22  
**版本**: v1.0.1 (修复了内插字符串转义问题)
